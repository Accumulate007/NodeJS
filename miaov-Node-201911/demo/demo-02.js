/**
 * global
 * 
 * 
 * 
 */

// 返回当前模块文件解析后的绝对路径(该属性并非全局属性，而是模块作用域下的)
console.log(__filename) // F:\Node-1125\demo\demo-02.js     返回的是文件的路径

// 返回当前文件模块所在目录解析后的绝对路径(该属性并非全局属性，而是模块作用域下的)
console.log(__dirname)  // F:\Node-1125\demo    返回的是文件夹的路径





